import Modal from "@mui/material/Modal";
import React from "react";

interface AddModalProps {
  openAdd: boolean;
}

const AddModal: React.FC<AddModalProps> = ({ openAdd }) => {
  return (
    <>
      <Modal>
        <div>skajdgfhhakjs</div>
      </Modal>
    </>
  );
};

export default AddModal;
